{
    "services": [
      {
        "name": "backend",
        "buildCommand": "cd path/to/backend && npm install && npm run build",
        "startCommand": "cd path/to/backend && npm run server"
      },
      {
        "name": "frontend",
        "buildCommand": "cd path/to/frontend && npm install && npm run build",
        "startCommand": "cd path/to/frontend && npm run dev"
      },
      {
        "name": "admin",
        "buildCommand": "cd path/to/admin && npm install && npm run build",
        "startCommand": "cd path/to/admin && npm run dev"
      }
    ]
  }
  